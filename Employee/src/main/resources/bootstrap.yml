spring:
  cloud:
    config:
      name: neo-config
      profile: dev
      discovery:
        enabled: true
        service-id: spring-cloud-config-server
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8000/eureka
